# Research Log: South Korea DART
**Generated**: 2026-02-24T14:25:00Z
**Status**: Complete

---

## 1. VERSION INFORMATION

| Component | Current (in requirements.txt) | Latest Available | Action Required |
|-----------|-------------------------------|------------------|-----------------|
| dart-fss | >=0.4 | 0.4.15 | OK (compatible) |

---

## 2. DOCUMENTATION SOURCES

### Primary Source -- dart-fss (Unofficial Wrapper)
- **URL**: https://github.com/josw123/dart-fss
- **Verified**: 2026-02-24
- **Version**: 0.4.15

### Secondary Source -- DART Open API (Government API)
- **URL**: https://opendart.fss.or.kr/api
- **Verified**: 2026-02-24 (via repo doc `DART Api doc`)
- **Key required**: Yes (free registration)

---

## 3. VERBATIM CODE SNIPPETS

### 3a. dart-fss API (verified current)
```python
# COPIED VERBATIM from dart-fss README
import dart_fss as dart
dart.set_api_key(api_key='xxxxxxxx')
corp_list = dart.get_corp_list()
samsung = corp_list.find_by_corp_name('samsung', exactly=True)[0]
fs = samsung.extract_fs(bgn_de='20120101')
```

### 3b. DART Open API Endpoints (Government)
```
GET /company.json?crtfc_key=KEY&corp_code=CODE       -- company info
GET /fnlttSinglAcntAll.json?crtfc_key=KEY&corp_code=CODE&bsns_year=YYYY&reprt_code=11011&fs_div=CFS
GET /list.json?crtfc_key=KEY&page_count=100           -- disclosure list
GET /exctvSttus.json?crtfc_key=KEY&corp_code=CODE     -- executives
```

---

## 4. WRAPPER CODE AUDIT

### Mode 1: dart-fss (Unofficial Wrapper) -- PRIMARY

| Method | Status | Issue |
|--------|--------|-------|
| `__init__` | OK | `dart_fss.set_api_key()` correct |
| `list_companies()` | OK | `dart_fss.get_corp_list()` + `.corps` iteration correct |
| `search_company()` | OK | `corp_list.find_by_corp_name(name, exactly=False)` correct |
| `get_profile()` | OK | corp attributes (`corp_name`, `stock_code`, `corp_code`) correct |
| `_fetch_via_dart_fss()` | OK | `corp.extract_fs(bgn_de=..., end_de=...)` correct |

### Mode 2: Direct DART API -- FALLBACK

| Method | Status | Issue |
|--------|--------|-------|
| `get_profile()` fallback | OK | `/company.json` endpoint correct |
| `_fetch_via_direct_api_financials()` | OK | `/fnlttSinglAcntAll.json` endpoint correct |
| `_search_via_direct_api()` | MINOR | `/list.json` used for search -- works but not ideal |
| `get_executives()` | OK | `/exctvSttus.json` endpoint correct |

### Canonical Field Coverage
- dart-fss `extract_fs()` returns comprehensive financial data
- `_DART_MAP` in canonical_translator provides Korean account name mappings
- Direct API fallback uses `thstrm_amount` (current period) for values -- correct

### Minor Issue
- `filing_date` in direct API fallback is set to `rcept_no` (receipt number) instead of actual filing date
- Should extract `rcept_dt` from the disclosure list for true PIT alignment

---

## 5. REQUIRED FIXES

1. **NONE critical** -- both modes are functional with correct API patterns
2. **MINOR**: filing_date in direct API should be actual date, not receipt number

---

## 6. IMPLEMENTATION READINESS

### Recommendation
- READY TO IMPLEMENT -- dart-fss API matches, gov API endpoints correct

---

**END OF RESEARCH LOG**
