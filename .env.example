# Operator 1 -- API Keys
# Copy this file to .env and fill in your actual keys.
#
# On Kaggle, use Kaggle Secrets instead (these are ignored on Kaggle).

# ---------------------------------------------------------------------------
# LLM Provider: Choose between Gemini and Claude (or leave empty to auto-detect)
# ---------------------------------------------------------------------------
# Supported values: "gemini" (default), "claude"
# If left empty, auto-detects based on which API key is available.
# Can also be set in config/global_config.yml via the llm_provider key.
# LLM_PROVIDER=gemini

# ---------------------------------------------------------------------------
# LLM Option 1: Gemini AI (default)
# ---------------------------------------------------------------------------
GEMINI_API_KEY=your_gemini_api_key_here

# ---------------------------------------------------------------------------
# LLM Option 2: Anthropic Claude (alternative to Gemini)
# ---------------------------------------------------------------------------
# Register at https://console.anthropic.com/ to get an API key.
# ANTHROPIC_API_KEY=your_anthropic_api_key_here

# ---------------------------------------------------------------------------
# US SEC EDGAR -- email identity (required by SEC regulation, not an API key)
# ---------------------------------------------------------------------------
EDGAR_IDENTITY=your.name@example.com

# ---------------------------------------------------------------------------
# Required: Market-specific API keys (all free registration)
# ---------------------------------------------------------------------------

# Japan J-Quants -- free email registration at https://jpx-jquants.com/login
# Replaces EDINET (which required website-hosted project for registration)
JQUANTS_API_KEY=your_jquants_api_key_here

# UK Companies House -- register at https://developer.company-information.service.gov.uk/
# COMPANIES_HOUSE_API_KEY=your_companies_house_key_here

# South Korea DART -- register at https://opendart.fss.or.kr/
# DART_API_KEY=your_dart_api_key_here

# ---------------------------------------------------------------------------
# Optional: Enhanced features (all free registration)
# ---------------------------------------------------------------------------

# OpenFIGI -- optional, for higher rate limits (works without key at lower rate)
# openfigi_key=your_openfigi_key_here

# US FRED macro data -- register at https://fred.stlouisfed.org/docs/api/api_key.html
# FRED_API_KEY=your_fred_key_here

# French INSEE macro data -- register at https://api.insee.fr/
# INSEE_API_KEY=your_insee_key_here

# Japan e-Stat macro data -- register at https://api.e-stat.go.jp/
# ESTAT_API_KEY=your_estat_key_here

# Korea KOSIS macro data -- register at https://kosis.kr/openapi/
# KOSIS_API_KEY=your_kosis_key_here

# Chile central bank macro data -- register at https://si3.bcentral.cl/
# BCC_API_KEY=your_bcc_key_here

# ---------------------------------------------------------------------------
# Supported PIT Markets (no API key needed unless noted above)
# ---------------------------------------------------------------------------
# US:     SEC EDGAR    -- NYSE/NASDAQ ($50T)    -- https://www.sec.gov/edgar (email identity only)
# EU:     ESEF/XBRL   -- Pan-EU ($8-9T)        -- https://filings.xbrl.org (no key)
# UK:     Companies House -- LSE ($3.18T)       -- key required (see above)
# Japan:  J-Quants     -- TSE ($6.5T)           -- key required (see above)
# Korea:  DART         -- KOSPI/KOSDAQ ($2.5T)  -- key required (see above)
# Taiwan: MOPS         -- TWSE/TPEX (~$1.2T)    -- https://mops.twse.com.tw (no key)
# Brazil: CVM          -- B3 ($2.2T)            -- https://dados.cvm.gov.br (no key)
# Chile:  CMF          -- Santiago ($0.4T)       -- https://www.cmfchile.cl (no key)
#
# Total coverage: $91+ trillion in market cap across 10 countries.
