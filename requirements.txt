# Operator 1 -- Python dependencies
# Core
requests>=2.28
pandas>=1.5
numpy>=1.23
pyarrow>=10.0
pyyaml>=6.0
python-dotenv>=1.0  # .env file support for local development

# Temporal modeling -- Phase 6
# T6.1: regime detection + causality
statsmodels>=0.14
scikit-learn>=1.2
ruptures>=1.1
hmmlearn>=0.3

# T6.2: forecasting models (actively used in forecasting.py)
arch>=6.0
xgboost>=1.7
torch>=2.0

# T6.1: Bayesian change-point detection (used in regime_detector.py)
pymc>=5.0

# Phase F: Advanced modules
shap>=0.42             # SHAP explainability (F2)
mapie>=0.8             # Conformal prediction alternative (F1)

# Phase G: DTW and uncertainty
dtaidistance>=2.3      # Dynamic Time Warping for historical analogs (G1)

# Not yet implemented -- uncomment when adding these modules:
# SALib>=1.4          # Sobol sensitivity analysis
# TA-Lib>=0.4         # Candlestick pattern detection
# deap>=1.4           # Genetic algorithm optimisation
# pywt>=1.4           # Wavelet decomposition

# Report generation (Phase 7, used in report_generator.py)
matplotlib>=3.6

# Not yet used:
# plotly>=5.13

# Phase 1: Unofficial wrappers for core markets
edgartools>=5.15            # US SEC EDGAR (primary) -- v5.15+ required: cash_flow()->cashflow_statement() rename
sec-edgar-api>=1.0         # US SEC EDGAR (fallback)
edinet-tools>=0.3          # Japan EDINET (primary) -- v0.3+ required: API changed (entity/search/documents)
dart-fss>=0.4              # South Korea DART (primary)
# pyesef -- REMOVED: library archived 2026-01-03 (graceful degradation in eu_esef_wrapper.py)
# pycvm -- optional: file-based API, br_cvm_wrapper.py uses direct HTTP as primary path
pycvm>=0.4                 # Brazil CVM data (optional, used for local file parsing)

# Phase 2: Tier 2 market wrappers (optional -- graceful degradation if not installed)
# akshare>=1.18             # China SSE/SZSE (comprehensive: stocks, financials, macro) -- research: .roo/research/cn-sse-2026-02-24.md
# jugaad-data>=0.29         # India NSE/BSE (stock data, RBI rates) -- research: .roo/research/in-bse-2026-02-24.md
# ixbrl-parse>=0.10         # UK Companies House iXBRL financial extraction -- research: .roo/research/unofficial-wrapper-discovery-2026-02-24.md

# Phase 2b: Privacy & NER (optional -- local PII detection without network calls)
# spacy>=3.5               # Local NER for personal data detection (12MB model: en_core_web_sm)
# After installing: python -m spacy download en_core_web_sm

# Phase 3: Macro data libraries
fredapi>=0.5               # US FRED (replaces manual HTTP calls)
wbgapi>=1.0                # World Bank global fallback (200+ countries)
sdmx1>=2.0                 # SDMX: ECB, OECD, Eurostat, INSEE (successor to pandasdmx, pydantic v2 compatible)
# imf-reader>=1.4           # IMF global macro fallback (190+ countries) -- research: .roo/research/macro-imf-reader-2026-02-24.md

# Testing
pytest>=7.0
